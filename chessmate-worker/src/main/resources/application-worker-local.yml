spring:
  config:
    activate:
      on-profile: worker-local
  application:
    name: ChessMate-worker

chessmate:
  sync:
    chesscom:
      parallelism: 5

logging:
  level:

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus
  endpoint:
    prometheus:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true