# fix-134: GameStatAggregator 증분 집계 전환

## 문제

증분 sync(30분 스케줄)에서 신규 게임이 1건이라도 있으면 `aggregate(userId, platform)`이 호출되어
**Game 테이블 전체**(최대 수만 건)를 DB에서 읽은 뒤 stat 테이블 3개를 전부 delete → 재계산 → saveAll 하는 구조였다.

```
30분 스케줄 → 신규 게임 5건 저장 → gameRepository.findByUserIdAndPlatform() (50,000건 로드)
  → dailyStatRepository.deleteByUserIdAndPlatform() + saveAll()
  → colorStatRepository.deleteByUserIdAndPlatform() + saveAll()
  → firstMoveStatRepository.deleteByUserIdAndPlatform() + saveAll()
```

## 원인

`aggregate()`가 `(userId, platform)` 만 받아 전체 재계산하는 방식으로 설계되어 있었고,
Worker에서 이미 `saveAll()` 반환값(`List<Game> saved`)을 가지고 있었음에도 이를 집계에 넘기지 않았다.

## 수정 내용

### 1. Domain 레포지토리 — findByKey 메서드 추가

- `UserDailyGameStatRepository`: `findByUserIdAndPlatformAndDate()`
- `UserColorStatRepository`: `findByUserIdAndPlatformAndTimeClassAndColor()`
- `UserFirstMoveStatRepository`: `findByUserIdAndPlatformAndTimeClassAndColorAndMove()`

### 2. JPA 레포지토리 — Spring Data 파생 쿼리 추가 (동일 메서드명 3개)

### 3. Repository 구현체 — 위임 메서드 추가 (3개)

### 4. GameStatAggregator — `aggregateIncremental()` 추가

```java
public void aggregateIncremental(Long userId, OAuthPlatForm platform, List<Game> newGames)
```

- 신규 게임만 받아 기존 stat 행을 찾아 카운터를 더함 (없으면 신규 생성)
- DB 접근: Game 전체 로드 없음, 신규 게임의 unique key 수만큼만 stat 조회
- 기존 `aggregate()` (full recompute)는 초기 전체 수집(`processFullSync`) 및 긴급 재집계용으로 유지

### 5. LichessGameSyncWorker — `processIncremental()` 수정

- `totalNewlySaved: int` → `allNewlySaved: List<Game>` 로 변경하여 저장된 게임 누적
- `aggregate()` → `aggregateIncremental(userId, platform, allNewlySaved)` 호출

### 6. ChessComGameSyncWorker — 수정

- `fetchAndSave()`: `void` → `List<Game>` 반환
- `processParallel()`: `void` → `List<Game>` 반환 (CompletableFuture<List<Game>> 사용)
- `processSequential()`: `void` → `List<Game>` 반환
- `process()`: 반환된 게임 목록으로 `aggregateIncremental()` 호출

### 7. user_monthly_rating_stat 테이블 신설 (rating-history O(N²) 쿼리 대체)

**문제**: `getRatingHistory()`가 `Game` 테이블에서 `(timeClass, year, month)` 그룹별 마지막 게임을 찾는
코릴레이티드 서브쿼리를 사용 → YEAR()/MONTH() 함수 사용으로 인덱스 미사용, O(N²) 성능

**해결**: 월별 레이팅 스냅샷을 별도 테이블에 사전 집계

신규 파일:
- `chessmate-domain/.../stat/UserMonthlyRatingStat.java`
- `chessmate-domain/.../stat/UserMonthlyRatingStatRepository.java`
- `chessmate-infra-persistence/.../entity/UserMonthlyRatingStatJpaEntity.java`
  - unique constraint: `(user_id, platform, time_class, year, month)`
- `chessmate-infra-persistence/.../jpaRepository/UserMonthlyRatingStatJpaRepository.java`
- `chessmate-infra-persistence/.../mapper/UserMonthlyRatingStatMapper.java`
- `chessmate-infra-persistence/.../repositoryImpl/UserMonthlyRatingStatRepositoryImpl.java`

### 8. GameStatAggregator — monthly rating 집계 추가

- `isAnyStatEmpty()`: `monthlyRatingStatRepository.existsByUserIdAndPlatform()` 체크 추가
- `aggregate()`: `computeMonthlyRatingStat()` 호출 추가
  - 전체 게임을 `(timeClass, year, month)` 키로 그룹화
  - 그룹당 `playedAt` 최신 게임의 `rating`을 저장
  - delete + saveAll
- `aggregateIncremental()`: `incrementMonthlyRatingStat()` 호출 추가
  - 신규 게임을 동일 키로 그룹화
  - 기존 행이 있으면 rating 교체(신규 게임이 항상 더 최신), 없으면 신규 INSERT

### 9. Worker 디스패치 로직 수정 (긴급 복구 자동화)

기존 문제: `isAnyStatEmpty() == true && newGames.isEmpty()` 이면 `aggregateIncremental(emptyList)`
→ rated.isEmpty() 조기 반환 → stat 테이블이 빈 채로 방치

```java
// 수정 전
if (!allNewlySaved.isEmpty() || statAggregator.isAnyStatEmpty(...)) {
    statAggregator.aggregateIncremental(..., allNewlySaved);
}

// 수정 후
if (statAggregator.isAnyStatEmpty(...)) {
    statAggregator.aggregate(...);          // 전체 재집계 (Game 테이블 기반)
} else if (!allNewlySaved.isEmpty()) {
    statAggregator.aggregateIncremental(..., allNewlySaved);  // 증분
}
```

적용 파일: `LichessGameSyncWorker.processIncremental()`, `ChessComGameSyncWorker.process()`

**배포 시 자동 백필 흐름**: `user_monthly_rating_stat` 테이블이 비어있음
→ `isAnyStatEmpty()` true → 다음 스케줄 실행 시 `aggregate()` 호출 → Game 테이블 기반 전체 재집계

### 10. StatService.getRatingHistory() — 신규 테이블로 교체

```java
// 수정 전: 코릴레이티드 서브쿼리
gameRepository.findMonthlyLastRating(userId, platform, timeClass, since)

// 수정 후: 단순 인덱스 조회
monthlyRatingStatRepository.findByUserIdAndPlatform(userId, platform)
    .stream()
    .filter(timeClass 및 12개월 범위 필터)
```

`GameRepository` 의존성 제거, `LocalDateTime` import 제거

## 변경 파일

| 파일 | 변경 |
|---|---|
| `chessmate-domain/.../UserDailyGameStatRepository.java` | findByKey 추가 |
| `chessmate-domain/.../UserColorStatRepository.java` | findByKey 추가 |
| `chessmate-domain/.../UserFirstMoveStatRepository.java` | findByKey 추가 |
| `chessmate-domain/.../UserMonthlyRatingStat.java` | 신규 |
| `chessmate-domain/.../UserMonthlyRatingStatRepository.java` | 신규 |
| `chessmate-infra-persistence/.../UserDailyGameStatJpaRepository.java` | Spring Data 메서드 추가 |
| `chessmate-infra-persistence/.../UserColorStatJpaRepository.java` | Spring Data 메서드 추가 |
| `chessmate-infra-persistence/.../UserFirstMoveStatJpaRepository.java` | Spring Data 메서드 추가 |
| `chessmate-infra-persistence/.../UserDailyGameStatRepositoryImpl.java` | 위임 메서드 추가 |
| `chessmate-infra-persistence/.../UserColorStatRepositoryImpl.java` | 위임 메서드 추가 |
| `chessmate-infra-persistence/.../UserFirstMoveStatRepositoryImpl.java` | 위임 메서드 추가 |
| `chessmate-infra-persistence/.../UserMonthlyRatingStatJpaEntity.java` | 신규 |
| `chessmate-infra-persistence/.../UserMonthlyRatingStatJpaRepository.java` | 신규 |
| `chessmate-infra-persistence/.../UserMonthlyRatingStatMapper.java` | 신규 |
| `chessmate-infra-persistence/.../UserMonthlyRatingStatRepositoryImpl.java` | 신규 |
| `chessmate-worker/.../GameStatAggregator.java` | aggregateIncremental + 5개 헬퍼 추가, monthly rating 집계 |
| `chessmate-worker/.../LichessGameSyncWorker.java` | processIncremental 수정 (디스패치 로직 포함) |
| `chessmate-worker/.../ChessComGameSyncWorker.java` | fetchAndSave/processParallel/processSequential/process 수정 |
| `chessmate-api/.../StatService.java` | getRatingHistory 신규 테이블 사용, GameRepository 의존 제거 |

## 성능 비교

| | 수정 전 | 수정 후 |
|---|---|---|
| 증분 집계 DB 읽기 | Game 전체 (N건) | 신규 게임 unique key 수 (≈ stat 조회 3~10건) |
| 증분 집계 stat 쓰기 | delete + saveAll 전체 | 변경된 행만 saveAll |
| 증분 집계 복잡도 | O(전체 게임 수) | O(신규 게임 수) |
| rating-history 쿼리 | O(N²) 코릴레이티드 서브쿼리 | O(1) 인덱스 조회 (user_id, platform) |

---

## 코드 리뷰 지적사항 (Copilot, 2026-04-27)

### [P1] `year`/`month` 컬럼명 예약어 문제 — `UserMonthlyRatingStatJpaEntity`

**지적**: `@Column(name = "`year`")` backtick 처리가 Hibernate/dialect 종속적이며, 프로덕션 테이블이 quoting 없이 생성됐을 경우 `ddl-auto=validate` 시작 실패 가능.

**분석**:
- H2 테스트에서 `year`/`month`가 예약어로 인식돼 DDL 실패 → backtick quoting으로 임시 해결
- 실제 MySQL 프로덕션에서는 `year`/`month`가 예약어가 아니므로 테이블이 plain 컬럼명으로 생성됨
- Hibernate가 `ddl-auto=validate`로 스키마를 검증할 때 quoted name(`\`year\``)과 unquoted name(`year`)이 불일치할 위험 있음
- `@UniqueConstraint(columnNames = {"`year`", "`month`"})` 도 동일 문제

**해결 방안**: 컬럼명을 예약어가 아닌 이름으로 변경
```java
// Before
@Column(name = "`year`")   private int year;
@Column(name = "`month`")  private int month;

// After
@Column(name = "stat_year")   private int year;
@Column(name = "stat_month")  private int month;
```
`@UniqueConstraint`, `@Index(columnList = ...)` 도 동일하게 변경.
프로덕션 DDL이 이미 `year`/`month`로 생성돼 있다면 컬럼 rename 마이그레이션 스크립트 필요.

---

### [P2] `getRatingHistory()` 정렬 누락 — `StatService`

**지적**: `findByUserIdAndPlatform()` 결과를 정렬 없이 stream 처리 → API 응답 순서 비결정적. 이전 쿼리는 YEAR/MONTH ORDER BY를 포함했음.

**분석**:
- DB 조회 결과는 저장 순서나 pk 순서로 반환되며 보장되지 않음
- 프론트엔드에서 월별 레이팅 차트를 시간순으로 렌더링하므로 순서가 흐트러지면 그래프가 깨짐

**해결 방안**:
```java
.filter(s -> {
    YearMonth ym = YearMonth.of(s.getYear(), s.getMonth());
    return !ym.isBefore(oneYearAgo) && !ym.isAfter(now);
})
.sorted(Comparator
    .comparing((UserMonthlyRatingStat s) -> YearMonth.of(s.getYear(), s.getMonth()))
    .thenComparing(UserMonthlyRatingStat::getTimeClass))
.map(s -> new MonthlyRatingEntry(...))
.toList();
```

---

### [P3] `allNewlySaved` 대규모 메모리 누적 — `LichessGameSyncWorker` / `ChessComGameSyncWorker`

**지적**: 두 워커 모두 모든 페이지(청크/아카이브)에서 저장된 Game 객체를 `allNewlySaved`에 누적한 뒤 마지막에 한 번 `aggregateIncremental()` 호출. 장기 미동기화 유저의 경우 수천 건이 메모리에 쌓임.

**분석**:
- 정상 30분 증분 경로: 신규 게임 수 소량 → 문제없음
- 장기 미접속 후 복귀 유저 (`isAnyStatEmpty() == false`, 신규 게임 수백~수천 건): `allNewlySaved`가 Game 엔티티 전체를 힙에 유지한 채 수집 완료까지 GC 불가
- `isAnyStatEmpty() == true` 경로에서는 `aggregate()`(전체 재집계)로 분기되므로 `allNewlySaved`가 사용되지 않음에도 메모리를 점유

**해결 방안 (청크별 즉시 집계)**:
```java
// Lichess: 루프 내에서 chunk 단위 집계
List<Game> saved = gameRepository.saveAll(toSave);
if (!saved.isEmpty()) {
    statAggregator.aggregateIncremental(userId, platform, saved);
}
// allNewlySaved 리스트 불필요 → 제거

// ChessComGameSyncWorker: fetchAndSave() 내에서 또는 archive 단위로 즉시 집계
```
단, 이 변경 시 `isAnyStatEmpty()` 체크 시점 조정 필요 (루프 전 1회 확인 → full/incremental 경로 결정 후 고정).

---

### [P4] 타이밍 단언 flakiness — `RatingHistoryQueryPerformanceTest`

**지적**: `assertThat(elapsedMs).isLessThan(100L)` — CI 환경(공유 러너, 콜드 JVM, H2 초기화 오버헤드)에서 간헐적 실패 가능.

**분석**:
- H2 in-memory + `@DataJpaTest`는 36,000건 삽입 후 인덱스 조회를 수행하므로 보통 수 ms 이내
- 그러나 CI 환경에서는 JVM warm-up 없이 실행되고 CPU 경합이 발생할 수 있어 100ms 초과 가능

**해결 방안**: 타이밍 단언 제거, 결과 건수 검증만 유지 (성능 의도는 주석으로 남김)
```java
// 성능 의도: 인덱스 조회로 O(1) — 수치 단언은 CI 환경 의존성으로 제거
System.out.printf("[PERF] 36,000건 중 단일 유저 조회: %dms | 결과 rows: %d%n", elapsedMs, result.size());
assertThat(result).hasSize(36);
// assertThat(elapsedMs).isLessThan(100L);  ← CI flaky, 제거
```